package com.example.haha

import androidx.compose.ui.graphics.Color

/** Shared color tokens used across more than one screen. */
internal object AppColors {
    val Background  = Color(0xFF09090B)  // Zinc950 — app/screen background
    val Surface     = Color(0xFF18181B)  // Zinc900 — cards, sheets
    val TextMuted   = Color(0xFF71717A)  // Zinc500 — secondary/hint text
    val Accent      = Color(0xFF9333EA)  // Purple600 — primary CTA
    val AccentLight = Color(0xFFC084FC)  // Purple400 — active states, indicators
    val AccentPink  = Color(0xFFDB2777)  // Pink600  — gradient end
}
